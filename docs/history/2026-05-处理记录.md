# 2026-05 处理记录归档

本文件从 `00_项目枢纽.md` 迁出历史流水账。默认任务只读项目枢纽；只有需要追溯具体决策、PR、bug 或用户明确要求时才读取本归档。

## 2026-05-15

- 用户反馈用 `paper-webpage-builder` 的完整新旧 prompt 测试 demo 时，“推断的 Schema”为空。本轮计划增强 `demo.html` 的自然语言输出字段推断：识别 `Output Expectations` / `final response include` 这类段落中的 bullet list，并将常见描述映射为稳定字段名，如 `index_html_path`、`modules`、`assets`、`keywords`、`quality_checks`、`validation`、`risks`。
- 完成 demo Schema 推断增强：`extractOutputFields` 现在能识别 `Output Expectations` / `final response` / `include` 段落中的自然语言 bullet list，并将 `index.html path`、modules、figures/tables、keywords/tags、quality checks、validation、risks 映射为稳定字段名。风险语义同步修正：新增 optional 输出字段标为“需审查 / minor”，字段移除或新增 required 字段才标为 Breaking。验证结果：用 `paper-webpage-builder` 的 `70d5845:SKILL.md` 与当前 `HEAD:SKILL.md` 调用 demo `analyze()`，Output Schema 显示 `index_html_path/modules/assets/keywords/quality_checks/validation/risks`，其中 `keywords` 为 NEW，风险为“需审查”。
- 用户反馈 SitHub demo 粘贴两份 prompt 后点击“分析变更”无响应。本轮计划修复 `demo.html` 前端脚本阻断问题：先定位按钮事件/JS 语法错误，修复后用本地 8766 静态服务验证页面可返回并脚本可解析。
- 完成 demo 无响应修复：`demo.html` 中 `extractVars` 的 `const re = ..., m;` 会触发浏览器 JS 语法错误，导致整段脚本不执行；已拆成 `const re = ...; let m;`。同时将 `risk-badge` 从 `outerHTML` 替换改为稳定更新 `className/textContent`，避免首次分析后移除 id 导致后续分析失效。验证结果：Node VM 脚本语法检查通过；用旧/新 prompt 片段调用 `analyze()` 能生成风险卡片、变更摘要和 diff；8766 服务返回最新 `demo.html`。
- 用户要求继续完成 PR #2 合并闭环，并用端口展示 SitHub demo。本轮计划通过 `xxllovemkm/paper-webpage-builder` 的 path-specific GitHub token 合并 PR #2，同步本地 `experiment/sithub-pr-loop`，再用静态 HTTP 服务展示 `/mnt/shared-storage-user/xuxinglong-p/SitHub/demo.html`，最后回填 PR/CI/demo 状态。
- 用户确认开始执行 P0。本轮计划先完成 F1：在真实试点 `/mnt/shared-storage-user/xuxinglong-p/paper-webpage-builder` 接入可重复运行的 `commands.run_case`，把 golden case 从仅 schema 校验提升到关键输出片段回归，更新 GitHub Actions 运行 `sit test --run`，并用本地命令验证失败/通过路径；完成后再进入 F2 walkthrough。
- 完成 F1 真实 runner 接入：`paper-webpage-builder/skill.yaml` 新增 `commands.run_case`，调用 `scripts/sit_run_case.py`；`tests/golden.jsonl` 的 3 条 case 升级为 `partial` match，并保留静态 `actual` fixture 让普通 `sit test` 继续通过；`.github/workflows/sit-ci.yaml` 新增 runner 行为回归步骤并把输出写入 `reports/ci/sit-test-run.txt` artifact；PR 模板同步加入 `sit test --run`。
- 验证结果：`python3 -m sit.cli validate /mnt/shared-storage-user/xuxinglong-p/paper-webpage-builder` 通过；`python3 -m sit.cli test /mnt/shared-storage-user/xuxinglong-p/paper-webpage-builder` 通过 3/3；`python3 -m sit.cli test /mnt/shared-storage-user/xuxinglong-p/paper-webpage-builder --run` 通过 3/3；坏 runner `python3 -c 'print(123)'` 会让 `sit test --run` 返回 1 并输出 0/3；`python3 -m unittest discover -s tests` 在 SitHub 通过 36 项；`git diff --check` 在 SitHub 和 `paper-webpage-builder` 均通过。报告产物已写入 `paper-webpage-builder/reports/real-runner-loop/`。
- 用户确认执行下一步。本轮计划完成 F2 本地 walkthrough：在 `paper-webpage-builder` 基于 F1 提交创建 `feature/add-keywords` 分支，新增 optional `keywords` 输出字段、同步 prompt/runner/golden，运行 validate/test/test --run/diff/pr-summary/ci-summary，并把完整流程沉淀到 `docs/walkthrough.md`；若 GitHub push 仍受权限阻断，则记录为远端协作剩余风险。
- 完成 F2 本地 walkthrough：`paper-webpage-builder` 先提交 F1 runner 接入为 `70d5845 test: add runner-backed golden tests`，随后创建 `feature/add-keywords`，新增 optional `keywords` schema 字段、更新 `SKILL.md` 输出期望、runner 和 3 条 golden case，并通过 `sit commit -m "schema: add keywords output field"` 创建 `1e7cfbf`。`sit commit` 输出 version gate 通过：`risk=review-required, required=minor, actual=minor, version=0.2.0->0.3.0`。
- 验证结果：`sit validate .`、`sit test .`、`sit test . --run` 均通过；`sit diff HEAD~1..HEAD` 输出 `SCHEMA output property added keywords (optional)`、`RISK review-required`；`sit pr-summary HEAD~1..HEAD` 输出 reviewer-ready 摘要；`sit ci-summary . --compare HEAD~1..HEAD` 可生成 GitHub summary。已新增 `docs/walkthrough.md`，并将 `paper-webpage-builder/reports/add-keywords-walkthrough/` 作为本地证据。初次 push 因 GitHub 403 写权限不足失败；用户提供 `xxllovemkm` token 后，本地为该 repo 设置 `credential.useHttpPath=true` 和 path-specific credential，随后成功推送 `experiment/sithub-pr-loop` 与 `feature/add-keywords`，并创建 PR `https://github.com/xxllovemkm/paper-webpage-builder/pull/2`。
- 完成 PR #2 合并闭环和 demo 展示：通过 GitHub API 合并 `https://github.com/xxllovemkm/paper-webpage-builder/pull/2`，merge commit `51fe1b4bd18027b6283663b8e9ae4f742ed1e255`；本地 `experiment/sithub-pr-loop` 已 `git pull --ff-only` 同步到 `origin/experiment/sithub-pr-loop`，`sit validate .` 和 `sit test . --run` 均通过。SitHub demo 已用 `python3 -m http.server 8766 --bind 0.0.0.0` 在 `/mnt/shared-storage-user/xuxinglong-p/SitHub` 启动，并通过 `curl --noproxy '*' http://127.0.0.1:8766/demo.html` 验证返回 200。

## 2026-05-14

- 用户要求执行下一步，即从 `skill git.md` 回到“行为回归测试”主线。本轮计划实现 `sit test` 的 runner 模式：支持 `skill.yaml` 中的 `commands.run_case` 或 CLI `--runner`，逐条 golden case 运行 Skill 生成 actual，再按现有 match mode 做回归判断；保持默认静态 schema/golden 测试兼容。
- 完成行为回归测试 Runner 第一版：`sit test` 新增 `--run`、`--runner`、`--timeout`，支持读取 `skill.yaml` 的 `commands.run_case` 命令模板；每条 golden case 会写入 `{input}` JSON，runner 输出 actual 到 `{output}` 或 stdout，随后复用 output schema 与 `schema_only/exact/partial/contains` 做验证。CLI 版本提升到 `0.17.0`。
- 验证结果：`python3 -m unittest discover -s tests` 通过 36 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.17.0`；新增测试覆盖 manifest runner、CLI `--runner` 覆盖、runner regression 失败和缺少 runner 配置的错误输出。
- 用户要求把 `sit` CLI 各命令含义和示例讲解保存成文档。本轮计划新增 `docs/cli-command-guide.md`，作为面向使用者的命令说明入口，并在项目枢纽登记该文档职责。
- 完成 CLI 命令指南文档沉淀：新增 `docs/cli-command-guide.md`，覆盖接入/观察、校验/测试、语义 diff/report、PR/CI、发布门禁、Git 透传和完整工作流示例；`git diff --check` 通过。
- 用户要求实现 `sit onboard`。本轮计划把 `paper-webpage-builder` 试点中的人工接入步骤自动化：识别已有 `SKILL.md`，补齐 `skill.yaml`、schemas、golden case、GitHub Actions、PR 模板和 onboarding reports；默认不覆盖已有文件，并在结束时输出 `sit doctor` 接入状态。
- 完成 `sit onboard` 第一版：新增 `sit/onboard.py` 和 CLI 命令 `sit onboard [path] [--name ...] [--version ...] [--remote ...] [--no-git] [--force] [--format text|json]`；命令要求已有 `SKILL.md`，会补齐 SitHub package surface、生成 onboarding Markdown/HTML 报告、初始化 Git 或添加 origin remote，并输出 doctor 状态。CLI 版本提升到 `0.16.0`。
- 验证结果：`python3 -m unittest discover -s tests` 通过 32 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.16.0`；临时 `SKILL.md` 项目执行 `sit onboard --remote ...` 后，`sit validate` 和 `sit test` 通过，`sit doctor` 输出 `Status: warn`，warning 仅来自新 Git 仓库尚未提交。
- 用户要求把 `paper-webpage-builder` 真实 PR loop 实验转化为 SitHub onboarding 能力。本轮计划新增试点复盘文档、已有 Skill 接入 quickstart，并实现 `sit doctor` 作为最小接入检查命令；同时更新项目路线，避免继续把复杂 schema 扩展作为默认下一步。
- 完成 onboarding 能力沉淀第一版：新增 `docs/pilots/paper-webpage-builder.md` 记录真实试点、PR loop、CI 失败/修复和产品发现；新增 `docs/quickstart-existing-skill.md` 说明已有 `SKILL.md` 项目如何接入 SitHub；新增 `sit doctor [package] --format text|json`，检查 Git、GitHub remote、manifest、validate、golden tests、GitHub Actions workflow 和 reports。CLI 版本提升到 `0.15.0`。
- 验证结果：`python3 -m unittest discover -s tests` 通过 29 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.15.0`；真实试点 `paper-webpage-builder` 的 `sit doctor` 输出 `Status: warn`，其中 Git dirty 为 warning，其余 GitHub remote、manifest、validate、golden tests、GitHub Actions workflow 和 reports 均通过。

## 2026-05-13

- 用户要求进入下一优先级任务：HTML report 交互增强。本轮计划增强 `sit report --format html` 的语义 diff 展示：增加风险筛选按钮、长 diff 折叠/展开、复杂 schema path 高亮和事件计数，同时保持静态 HTML 单文件输出；版本预计提升到 `0.14.0`。
- 完成 HTML report 交互增强第一版：`sit report --format html` 的 Semantic Diff 增加 All/Breaking/Changed/Info 筛选按钮、事件计数、长 diff 折叠/展开按钮、复杂 schema path badge 和内联无依赖 JS；CI HTML artifact 同步受益。CLI 版本提升到 `0.14.0`。
- 验证结果：`python3 -m unittest discover -s tests` 通过 27 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.14.0`；example 的 visual report 和 CI HTML artifact 均能看到 `data-filter="breaking"`、`Expand long diff`、`schema-path` 和内联交互脚本。
- 用户要求实现 CI 配置增强，版本进入 `0.13.0`。本轮计划增强 `sit ci-summary` 的 baseline/head/package-dir 参数，支持生成 JSON/HTML/Markdown artifact；更新 `sit init` 生成的 GitHub Actions 模板，使 PR CI 能在 package 子目录、可配置 baseline 下运行，并在失败时上传 report artifacts。
- 完成 CI 配置增强第一版：`sit ci-summary` 支持 `--baseline-ref`、`--head-ref`、`--package-dir`、`--artifact-dir`，可从仓库根目录对 Skill 子目录生成 PR summary 和 `sit-report.json` / `sit-report.md` / `sit-report.html` / `sit-summary.md`；`sit init` 的 GitHub Actions 模板新增 `SIT_PACKAGE_DIR`、`SIT_BASELINE_REF`、`SIT_HEAD_REF`、`SIT_ARTIFACT_DIR` 配置，并在 `always()` 下上传 `sithub-report` artifact。CLI 版本提升到 `0.13.0`。
- 验证结果：`python3 -m unittest discover -s tests` 通过 27 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.13.0`；example 的 `ci-summary --artifact-dir examples/paper-taxonomy-mapper-v0.2.0/reports/ci-v0.13.0` 已生成 `sit-summary.md`、`sit-report.json`、`sit-report.md`、`sit-report.html`。
- 用户要求实现复杂 schema 支持：`oneOf` / `allOf` / `$ref`。本轮计划在现有递归 schema diff 上加入本地 JSON Pointer 引用解析，支持 `#/definitions/...` 与 `#/$defs/...`，并比较 `oneOf` / `allOf` 分支的新增、删除、重排/内容变化；补充测试、生成可视化报告，版本提升到 `0.12.0`。
- 完成复杂 schema 支持第一版：`sit diff` 现在会解析本地 `$ref`，比较引用目标 schema，并识别 `oneOf` / `allOf` 分支新增、删除、重排和内容变化；example schema 已改为包含 `#/definitions`、`#/$defs`、`oneOf`、`allOf`，并重新生成 Markdown/HTML 报告。CLI 版本提升到 `0.12.0`。验证结果：`python3 -m unittest discover -s tests` 通过 26 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.12.0`；example validate/test 均通过，visual report 能看到 `oneOf branch removed`、`allOf branch added` 和 `$ref target changed`。
- 用户反馈 HTTPS token 推送已执行成功。本轮计划核对本地/远端同步状态，记录 GitHub remote 发布状态，清理已过期的 Git 初始化未决项；仅修改项目枢纽文档并追加一次文档提交。
- 完成 GitHub 远端同步状态核对：`git status --short --branch` 显示 `main...origin/main` 且无未提交代码改动，`origin` 指向 `https://github.com/OpenRaiser/SitHub.git`，`HEAD` 与 `origin/main` 均为 `04c1f3a docs: record HTTPS remote fallback`。本次回填仓库状态并移除“是否初始化 Git repo”的过期未决项。
- 用户要求将项目提交到 `git@github.com:OpenRaiser/SitHub.git`。本轮计划先运行回归验证，再执行初始提交，设置 `origin` 为该 SSH remote，并推送 `main`；若 SSH 鉴权或远端权限失败，则保留本地提交并报告阻断点。
- SSH 到 GitHub 在当前环境被网络阻断后，用户要求改用 HTTPS remote `https://github.com/OpenRaiser/SitHub.git` 上传。本轮计划将 `origin` 切换到 HTTPS 并尝试推送；若 GitHub 要求用户名/token，则保留本地提交并等待用户提供凭据。
- 用户要求初始化 `/mnt/shared-storage-user/xuxinglong-p/SitHub` 为 Git repo。本轮计划执行 `git init -b main`，不创建提交、不自动添加文件；完成后检查 `git status --short --branch` 并回填仓库状态。
- 完成 Git repo 初始化：`git init -b main` 已在 `/mnt/shared-storage-user/xuxinglong-p/SitHub` 创建 `.git/`，当前分支为 `main`，`git rev-parse --is-inside-work-tree` 返回 `true`；当前状态为 `No commits yet on main`，项目文件均为 untracked，尚未执行 `git add` 或 `git commit`。
- 用户要求按优先级继续优化。本轮计划执行“更友好的错误输出和 release note”：增强 commit/release version gate 的阻断信息，使其包含风险、required/actual bump、版本变化、关键语义变更和可执行修复建议；同时让 `sit release` 生成的 CHANGELOG 和 release report 包含 release risk、建议 bump、验证/测试摘要和语义 diff 摘要。
- 完成错误输出和 release note 优化第一版：version gate 阻断信息现在包含 risk、required/actual bump、版本变化、原因、修复建议、绕过提示和关键语义变更；`sit release` 生成的 CHANGELOG 和 release report 会写入 release risk、version gate、validation/golden tests 和 semantic changes 摘要。CLI 版本提升到 `0.11.0`。验证结果：`python3 -m unittest discover -s tests` 通过 24 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.11.0`；专项测试覆盖 gate 阻断文案和 release note/report 内容。
- 用户反馈“还没有一个可视化的结果展示，无法直观感受现有工作的作用”。本轮计划补齐本地可视化反馈面：为 `sit report` 增加 `--format html`，基于既有 `sit.report.v1` payload 生成静态 HTML dashboard，展示验证状态、golden test 进度、diff risk、suggested bump、语义变更列表和复现命令；同时为 example 生成一份可直接打开的可视化报告。
- 完成本地可视化反馈面第一版：`sit report` 支持 `--format markdown|json|html`，HTML report 将 package、validation、golden tests、diff risk、suggested bump、语义 diff timeline 和 reproduce commands 渲染成静态 dashboard；已生成 example 报告 `examples/paper-taxonomy-mapper-v0.2.0/reports/visual-v0.1.0-to-v0.2.0.html`。CLI 版本提升到 `0.10.0`。验证结果：`python3 -m unittest discover -s tests` 通过 24 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.10.0`；HTML 内容检查能看到 `breaking-change`、`Suggested bump` 和新增 required schema 字段。
- 用户要求继续下一步。本轮计划把机器可读信号接入 CI 展示面：新增 `sit ci-summary [package] --compare <range|package>`，基于 `sit.report.v1` payload 生成稳定 Markdown，可直接写入 GitHub `$GITHUB_STEP_SUMMARY`；同时更新 `sit init` 生成的 GitHub Actions 模板，让新 Skill Package 默认在 PR 中展示 validate/test/diff 风险摘要。
- 完成 GitHub Action summary 第一版：新增 `sit/ci.py` 和 `sit ci-summary [package] --compare <range|package> [-o path] [--append]`，基于 `sit.report.v1` payload 渲染 Markdown，包含 package、validation、golden tests、diff risk、suggested bump、semantic diff 和复现命令；`sit init` 生成的 `.github/workflows/sit-ci.yaml` 已更新为 fetch-depth 0，并在 PR CI 中执行 `sit ci-summary --compare origin/main..HEAD >> "$GITHUB_STEP_SUMMARY"`。CLI 版本提升到 `0.9.0`。验证结果：`python3 -m unittest discover -s tests` 通过 23 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.9.0`；example 的 `ci-summary --compare` 能输出 breaking-change 和 suggested major。
- 用户要求进行下一步。本轮计划补齐 CI/SitHub Web 可消费的机器输出：为 `sit test` 增加 `--format text|json`，输出 `sit.test.v1`；为 `sit report` 增加 `--format markdown|json`，输出 `sit.report.v1`，并保持现有 Markdown 默认行为和 `--output` 写文件行为兼容。
- 完成机器可读输出补齐第一版：`sit test` 支持 `--format text|json`，JSON schema 为 `sit.test.v1`；`sit report` 支持 `--format markdown|json`，JSON schema 为 `sit.report.v1`，包含 package、validation、golden_tests、可选 diff 和 reproducibility 命令；`sit info --format json` 已保持兼容。CLI 版本提升到 `0.8.0`。验证结果：`python3 -m unittest discover -s tests` 通过 21 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.8.0`；example 的 `test/report/info --format json` 均可输出机器可读状态。
- 用户要求进行下一步。本轮计划实现 commit/release gate：复用 `sit diff` 的 `suggested_bump` 作为误差信号，在有 Git HEAD 基线时比较当前包和基线包的版本变化；若 breaking change 未配套 major bump，或 review-required change 未配套至少 minor bump，则阻断 `sit commit` / `sit release`，并提供 `--no-version-gate` 作为显式绕过。
- 完成 commit/release gate 第一版：新增 `sit/gate.py`，通过 `git archive HEAD` 读取基线包并复用语义 diff 判断 required bump；`sit commit` 默认验证当前版本相对 HEAD 的实际 bump，`sit release` 默认验证传入 bump 是否满足语义 diff 要求；初始提交、非 Git 包或无 HEAD 基线时跳过 gate，`--no-version-gate` 可显式绕过。CLI 版本提升到 `0.7.0`。验证结果：`python3 -m unittest discover -s tests` 通过 19 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.7.0`；新增测试覆盖 breaking change + minor bump 被阻断、major release 放行。
- 用户要求继续完成“递归 schema diff 和 match mode test”。本轮计划提升语义误差信号深度：让 `sit diff` 递归识别 nested object、required、type、enum、items、additionalProperties 等 schema 变化；让 `sit test` 读取 golden case 的 `match_mode`，支持 `schema_only`、`exact`、`partial`、`contains`，保持旧 golden 文件默认兼容。
- 完成语义深度增强第一版：`sit diff` 递归比较 schema 节点，支持 nested object、required、type、enum、items、additionalProperties 和 min/max 类边界约束，并按兼容性标记 breaking/review-required；`sit test` 支持 `schema_only`、`exact`、`partial`、`contains` match mode，旧 golden case 默认按 `schema_only` 兼容，`exact/partial/contains` 会要求 `actual` 并先做 schema 校验。CLI 版本提升到 `0.6.0`。验证结果：`python3 -m unittest discover -s tests` 通过 16 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.6.0`；example 的 `sit test` 输出 `SUMMARY 3/3 golden cases passed`，`sit info --format json` 同步显示 3/3。
- 用户要求实现 `sit info`。本轮计划补齐本地控制回路的状态观测器：新增 `sit info [package] --format text|json`，聚合 package manifest、Git ref/dirty 状态、prompt/schema/test 清单、validate/test 摘要和最近 report；同时补充测试并修正进展文档中“下一步仍是统一输出格式”的漂移。
- 完成 `sit info` 第一版：新增 `sit/info.py`，CLI 支持 `sit info [package] --format text|json`，JSON schema 为 `sit.info.v1`，包含 package、Git、files、validation、golden_tests 和 reports；非 Git 目录会明确标记 Git unavailable，Git 工作区可显示 branch/commit/dirty 状态。CLI 版本提升到 `0.5.0`。验证结果：`python3 -m unittest discover -s tests` 通过 14 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.5.0`；example 的 `sit info --format text/json` 均能输出 v0.2.0 的 3/3 golden test 和最近 report 状态。
- 用户确认：CLI 名称继续使用 `sit`，平台名称正式使用 `SitHub`。本轮计划执行下一优先级“统一输出格式”：先让 `sit diff` 和 `sit pr-summary` 支持 `--format text|markdown|json`，其中 JSON 作为未来 SitHub Web/CI 的机器可读误差信号；同时回填命名决策，移除对应未决项。
- 完成输出格式统一第一版：`sit diff` 支持 `--format text|markdown|json`，`sit pr-summary` 支持 `--format markdown|text|json`；新增结构化 diff event，JSON 包含 `schema_version`、package refs、原始 source/ref、`risk`、`suggested_bump`、messages 和 events；PR summary JSON 进一步包含 validation、golden test 和嵌套 diff。CLI 版本提升到 `0.4.0`。验证结果：`python3 -m unittest discover -s tests` 通过 12 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.4.0`；example 的 `diff --format json/markdown` 和 `pr-summary --format json` 均能输出 breaking-change 信号。
- 用户要求“进行下一步”。本轮计划补齐闭环中的 Git ref 语义：让 `sit diff`、`sit pr-summary`、`sit report --compare` 支持 `main..HEAD` / `HEAD~1..HEAD` 这样的 Git range，同时保留直接比较两个 Skill Package 目录的旧用法；实现时通过临时快照读取 Git ref，不改动当前工作区。
- 完成 Git ref 支持第一版：新增 `sit/ref.py`，通过 `git archive` 将 `A..B` 两端 ref 展开到临时快照，再复用现有 package loader、diff、PR summary 和 report 逻辑；`sit diff HEAD~1..HEAD`、`sit pr-summary HEAD~1..HEAD`、`sit report --compare HEAD~1..HEAD` 已可用，旧的两个目录比较保持兼容；在 Git 仓库子目录中的 Skill Package 也能按当前包路径读取快照。CLI 版本提升到 `0.3.0`。验证结果：`python3 -m unittest discover -s tests` 通过 10 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.3.0`；example 目录对比版 `diff` / `pr-summary` 仍能识别 v0.1.0 到 v0.2.0 的 breaking schema change。
- 用户要求“按照优先级先完成闭环”。本轮计划实现第一版 `sit` 工作闭环：`sit init` 生成标准 Skill Package/GitHub 模板，Git 薄封装支持 `add/commit/push/pull/branch/checkout/log`，`sit commit` 前执行 validate/test gate，新增 `sit pr-summary` 生成 PR Markdown，新增 `sit release` 做 version bump、Git tag、CHANGELOG 和报告归档；同时补充测试并验证 example packages。
- 完成第一版本地闭环：新增 `sit/init.py`、`sit/git.py`、`sit/summary.py`、`sit/release.py`，扩展 `sit/cli.py`，实现 `sit init`、Git 薄封装、commit validate/test gate、`sit pr-summary`、`sit release`；CLI 版本提升到 `0.2.0`。补充 `tests/test_cli.py` 覆盖 init、PR summary、release、Git add/commit/log 闭环。验证结果：`python3 -m unittest discover -s tests` 通过 7 项测试，`python3 -m compileall -q sit tests` 通过，`python3 -m sit.cli --version` 输出 `sit 0.2.0`，example `pr-summary` 能生成 Markdown 并标记 `breaking-change`/建议 `major`。
- 用户指出现有 `00_项目枢纽.md`、`01_SIT_项目框架.md`、`02_SIT_项目说明与进展.md` 与目标不吻合，要求按照 `skill git.md` 并结合工程控制论学习思想重写整个项目。计划将项目重构为“Skill Git/SitHub 的控制论式版本协作系统”：`sit` 是本地控制器，Git 是底层历史存储，GitHub 是早期协作承载，SitHub 是后续平台形态。
- 完成核心文档重写：`00_项目枢纽.md` 改为控制论式项目入口，`01_SIT_项目框架.md` 改为 Skill Git/SitHub 系统架构，`02_SIT_项目说明与进展.md` 改为当前差距和下一轮实现计划；同步更新 `AGENTS.md` 的 Current Direction。验证结果：`python3 -m unittest discover -s tests` 通过 3 项测试，`python3 -m compileall -q sit tests` 通过，`sit diff` 示例仍能识别 v0.1.0 到 v0.2.0 的 required 字段新增并标记 `RISK breaking-change`。

## 2026-05-12

- 项目根目录从 `/mnt/shared-storage-user/xuxinglong-p/SIT` 切换到 `/mnt/shared-storage-user/xuxinglong-p/SitHub`，迁移可用 CLI 源码、测试、examples、打包配置和通用文档；旧 `/SIT` 不再作为工作目录。
- 项目从外部业务试点解耦，当前验证对象切换为 `examples/paper-taxonomy-mapper-v0.1.0` 与 `examples/paper-taxonomy-mapper-v0.2.0`。
- 为避免两个 `00_` 文件造成入口混淆，框架文档编号调整为 `01_SIT_项目框架.md`，`00_项目枢纽.md` 保持唯一入口。

## 历史记录

早期曾以 CMS-Bench 作为试点验证 `sit validate/test/diff/report`。该记录只作为历史探索，不再作为当前项目目标、实现前提或验证对象。
